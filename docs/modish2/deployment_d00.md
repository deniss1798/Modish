# D00 — выполненное обновление VPS

**RESULT: completed — базовое обновление GitHub.** Пользователь явно разрешил deployment на `194.87.118.236` под `root`. Успешное подключение выполнено отдельным SSH-клиентом; первоначальные отказы относились к терминальному способу ввода. Пароль в файлах проекта не сохранялся.

## Установленная версия

- DEPLOYED_SHA: `da5a0be1ae96b9656ede7d97dee3799ff3b4c5ca`.
- Image: `modish-backend:github-da5a0be1-d00`.
- Image ID: `sha256:195295b64a9a2628e97e923f530c80ed28329fe21e71448ebd49653d91c2befe`.
- DB_REVISION: `20260824_0019`, ранее `20260524_0014`.
- Переключение: 12.09.2026, 18:52:12–18:52:18 UTC; миграции заняли 1,98 с.
- Compose project: `modish`; сервис API: `backend`; контейнер: `modish_backend`.
- Production PostgreSQL и Redis не пересоздавались. Сохранены bind directories `/opt/Modish/postgres_data` и `/opt/Modish/redis_data`.
- Git blob hashes **109 файлов** работающего контейнера совпали с целевым commit.

Исходный checkout `/opt/Modish` и его пользовательские правки сохранены. Его HEAD остаётся `3ac3b1e4076956f15ac5dda5fb8438a36c09f016` и **не является версией работающего API**. Новое приложение собрано из чистого Git export в `/opt/modish-releases/20260912T110617Z-da5a0be1/backend`. В серверном `docker-compose.yml` старый build заменён проверенным image; повторная обычная сборка из старого checkout не подменит release.

Локальные T01-изменения ветки `codex/modish-2-3-foundation` и отсутствующий в этой копии патч размеров не входят в этот выпуск. Это отдельные следующие пакеты по порядку руководства. Известные продуктовые дефекты базового main не получают статус исправленных из-за deployment.

## Резервные копии и возврат

Каталог с ограниченным доступом: `/var/backups/modish/20260912T110617Z-da5a0be1/`.

- `database.dump`: 164 743 578 байт; восстановлен в отдельный PostgreSQL 16.
- `pre-upgrade.dump`: свежая копия перед переключением, 164 743 578 байт.
- `checkout-and-config.tar.gz`: исходный код, серверные правки, конфигурация и фиды; live data directories исключены из tar, БД сохранена логическим dump.
- `previous-image.tar`: прежний image, дополнительно сохранён tag `modish-backend:rollback-20260912T110617Z-da5a0be1`.
- `tls-and-nginx.tar.gz`: прежняя HTTPS-конфигурация и сертификаты.
- Приватные container inspect snapshots и metadata сохранены на сервере; их не публиковать, поскольку environment содержит секреты.

Release record и сценарии: `/opt/modish-releases/20260912T110617Z-da5a0be1/`.

```sh
# Возврат только приложения, без downgrade/удаления данных:
/opt/modish-releases/20260912T110617Z-da5a0be1/rollback.sh

# Повторное включение этого проверенного release:
/opt/modish-releases/20260912T110617Z-da5a0be1/release.sh
```

Возврат старого приложения на мигрированной копии БД проверен. Оба сценария сохраняют project, сеть и environment и запускают Uvicorn напрямую, без автоматического Alembic. Миграция выполнена отдельным одноразовым контейнером. После rollback последующие ручные Compose-операции должны использовать rollback override, пока оператор не решит вернуть новый release.

Удаление production volume, слепой `stamp`, downgrade или восстановление старого dump поверх новых записей не являются стандартным возвратом. Полный disaster recovery после новых пользовательских записей требует отдельного плана данных.

## Проверки

| Проверка | Результат |
|---|---|
| Восстановление dump в отдельный PostgreSQL 16 | прошло |
| Upgrade восстановленной схемы 0014 → 0019 и повторный upgrade | прошли |
| Исходные users / products / outfits до и после миграции | 11 / 183543 / 33, сохранены |
| Регистрация, конфликт дубликата, login, профиль, товары, detail, feed, save/unsave на копии | прошли |
| Лента на полном восстановленном каталоге | прошла |
| Предыдущий image с новой схемой на отдельной копии | прошёл тот же основной smoke |
| Suite внутри нового Linux image без сети, тестовая SQLite | 70 тестов прошли |
| `.env*` в новом image | отсутствуют; исключены при упаковке чистого export |
| Live `/health` и `/products?limit=1` локально и через HTTPS | 200, database=ok, непустой каталог |
| Live `/feed` без токена | 401 |
| HTTPS-запросы с локального компьютера | прошли |
| Исходные строки production после deployment | 11 пользователей, 183543 товара, 33 образа |

Проверки с записью выполнялись на отдельной восстановленной БД, не на production. Установка APK и ручной сценарий на телефоне в этой сессии не выполнялись. Smoke не доказывает правильность всех цен, размеров, атрибуции или готовность внешней альфы.

Тестовый PostgreSQL, его отдельный anonymous volume и внутренняя Docker network после проверки удалены. Production данные и резервные копии сохранены. Вспомогательные runtime env файлы удалены. Скрипт воспроизводимого smoke — `deploy/smoke_d00.py`; он блокирует запуск при другом host/database и требует явного `MODISH_ISOLATED_SMOKE=1`.

## Исправленный HTTPS

Обнаружен сертификат `modish.org.ru`, истёкший **16.08.2026**. Раньше использовался manual authenticator, поэтому включённого timer было недостаточно для продления.

Сертификат обновлён через Nginx, срок действия — **11.12.2026**. Renewal переведён на Nginx; existing `certbot.timer` включён и активен. Добавлен deploy hook с проверкой и reload Nginx. `certbot renew --dry-run --run-deploy-hooks --no-random-sleep-on-renew` прошёл. Nginx config test и внешняя проверка доверенной TLS-цепочки прошли.

Основной runtime endpoint: https://modish.org.ru/health.
