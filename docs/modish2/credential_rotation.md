# T01 — внешние действия и ограничения

12.09.2026. Найденные значения не проверялись запросами к провайдерам. Действительность, использование и отзыв неизвестны.

1. Отозвать ранее опубликованный ключ AI у соответствующего провайдера; проверить журнал использования и выставленные расходы.
2. Отозвать/заменить экспортный access code Admitad. Новый код не передавать в чат и не коммитить.
3. Внести новые credentials только в защищённую конфигурацию нужной среды; обновить ранее сохранённые credential-bearing `ProductSource.feed_url` в рамках отдельной операции с БД. До этого оставить интеграцию выключенной и не запускать старые sync по сохранённым URL.
4. Проверить, не остались ли прежние значения в серверных логах, артефактах, резервных копиях и копиях репозитория. Ограничить доступ и сроки хранения по фактическим процессам проекта.
5. Зафиксировать дату отзыва и среду установки новых значений без самих значений. Переписывание Git history не выполнялось и не заменяет отзыв.

## Маскированные сведения из истории

| Commit | Путь | Строка | Тип |
|---|---|---|---|
| `2beecc26dcbe929f52f3fec904751c8bcb68b223` | `backend/app/integrations/admitad/source_presets.py` | 9 | credential_url |
| `c8c7c9c74e1512908ebffe8434c72bd28323a504` | `backend/app/integrations/admitad/source_presets.py` | 25 | credential_url |
| `c8c7c9c74e1512908ebffe8434c72bd28323a504` | `backend/app/integrations/admitad/source_presets.py` | 36 | credential_url |
| `bd534cf9f7edd8ef872430e379ba510310e7e5d5` | `backend/.env.example` | 26 | provider_api_key |

Это четыре найденные записи добавленных строк, не четыре подтверждённых действующих ключа. Scanner проверяет известные форматы и не доказывает отсутствие произвольных паролей. Локальные пользовательские диагностические файлы в эту очистку не включались; значения в них не публиковать.

## Новая конфигурация

`ADMITAD_ENABLED=false` по умолчанию. Включение требует `ADMITAD_WEBSITE_ID`, `ADMITAD_EXPORT_USER`, `ADMITAD_EXPORT_CODE`. URL строится через HTTPS и URL encoding, сеть при тестах не вызывается. Реальная совместимость endpoint с текущим партнёрским контрактом ещё не проверена.

Отключение блокирует как bootstrap, так и сетевой sync источников `network=admitad`, включая ранее сохранённые URL. Локальный импорт тестового текста остаётся доступным. Другие сети этим флагом не управляются.

`APP_ENV=production` включает проверки обязательной конфигурации PostgreSQL и JWT. Примеры `REPLACE_ME` не являются рабочими credentials. Текущая конфигурация действующего сервера не менялась.
